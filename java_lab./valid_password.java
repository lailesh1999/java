public class valid_password {
    static void valid_password(String pass) {
        if (pass.length() < 8) {
            System.out.println("INVALID PASSWORD LESS PASSWORD");
        } else {
            int n = 0;
            int c1 = 0;
            char c;
            for (int i = 0; i <= pass.length(); i++) {
                c = pass.charAt(i);
            }
        }
    }
}
